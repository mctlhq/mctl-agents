# Atomic environment ReleaseBinding compatibility fixture — mctlhq/mctl-agents#227
# declarative resolver pilot. Mirrors the shape ADR 007 sec. 4 assigns to
# mctl-api's real ReleaseBinding once mctlhq/mctl-gitops#950 lands. This file
# is not that registry: bindingSource/promotable below say so explicitly, and
# orchestrator/resolver.py refuses to treat it as anything else.
#
# `definition.version` / `profile.version` pin the exact sha256 content hash
# of, respectively, agents/_manifests/issue-investigator/agent.yaml and
# tests/fixtures/resolver/profiles/investigator-default.yaml at the time this
# binding was written. orchestrator/resolver.py's execute() recomputes both
# hashes on every resolution and fails closed if either file has drifted out
# from under this binding without also updating it here — the same
# fail-before-submit discipline a real ReleaseBinding gets from mctl-api's
# atomic promote(), just enforced by content hash instead of a database row.
bindingSource: compatibility-fixture
promotable: false
environment: production
definition:
  name: issue-investigator
  version: "sha256:da51493e4573937a57c48ae0115abf605131f1f5bc502e3862bd4d08960d0a02"
profile:
  name: investigator-default
  version: "sha256:ea35387723c6837b6aef275617db7cbf52f145f9619c58f6217c7a2c0ccd8f2f"
releaseRevision: 1
registryLifecycle:
  definition: published
  profile: published
